<!-- <div class='attachments'>
  <%= render :partial => 'attachment', :collection => @attachments %>
</div> -->
<br/>
<div class="note_message">
    Добавляйте новые файлы и они автоматически будут видны сотрудникам фирмы.
</div>

<style>
#feedback { font-size: 1.4em; }
#selectable .ui-selecting { background: #FECA40; }
#selectable .ui-selected { background: #F39814; color: white; }
#selectable { list-style-type: none; margin: 0; padding: 0; }
#selectable li { margin: 3px; padding: 1px; float: left; width: 200px; height: 200px; font-size: 10px; text-align:left; }
</style>

<%= semantic_form_for(@attachment) do |f| %>

    <%= f.inputs do %>
        <%= f.file_field :attach %>
    <% end %>

    <br/>
    <!-- <%= f.buttons do %> -->
       <%= button_to 'Сохранить', @attachment, :method => :create, :class => 'button-styled blue', :align=>"light" %>
    <!-- <% end %> -->

    <br/>
    <%= unless @attachment.id.nil?
        link_to 'Destroy', event_path(@attachment), :confirm => 'Are you sure?', :method => :delete
       end
    %>
    <ol id="selectable">
        <% @attachments.each do |attachment| %>
            <li class="ui-state-default">
              <%= link_to attachment.attach_file_name, attachment.attach.url(:original, false) %>
              <%= image_tag attachment.attach.url, :width => 200, :height => 200 %>
            </li>
        <% end %>
    </ol>
    <%= raw(Helper.get_chat_script(current_user)) %>
<% end %>
